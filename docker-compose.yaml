version: '3'
services:

  spring-boot-app:
    build: 
      context: ./FirstApp
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

  react-app:
    build: 
      context: ./FirstApp-frontend/first-app
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
